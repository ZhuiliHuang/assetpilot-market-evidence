name: Update market data

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Fixture mode remains the only accepted mode before end-to-end acceptance"
        required: true
        default: fixture
        type: choice
        options:
          - fixture
      publish:
        description: "Publish the validated tree to the data branch"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: market-evidence-publication
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out trusted workflow code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install locked dependencies
        run: python -m pip install -e ".[dev]"
      - name: Run repository and calculation tests
        run: python -m pytest -q
      - name: Build fixture evidence in an isolated directory
        run: python scripts/update_market_data.py --fixtures --output "$RUNNER_TEMP/public"
      - name: Upload validated public tree
        uses: actions/upload-artifact@v4
        with:
          name: validated-market-evidence
          path: ${{ runner.temp }}/public
          if-no-files-found: error
          retention-days: 3

  publish:
    if: ${{ inputs.publish }}
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Check out trusted workflow code
        uses: actions/checkout@v4
      - name: Download validated public tree
        uses: actions/download-artifact@v4
        with:
          name: validated-market-evidence
          path: ${{ runner.temp }}/public
      - name: Publish only the validated public directory
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git clone --branch data --single-branch "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "$RUNNER_TEMP/data-branch"
          rm -rf "$RUNNER_TEMP/data-branch/public"
          cp -R "$RUNNER_TEMP/public" "$RUNNER_TEMP/data-branch/public"
          cd "$RUNNER_TEMP/data-branch"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public
          git diff --cached --quiet && exit 0
          git commit -m "data: publish validated market evidence"
          git push origin data

  deploy:
    if: ${{ inputs.publish }}
    needs: publish
    uses: ./.github/workflows/deploy-pages.yml
    permissions:
      contents: read
      pages: write
      id-token: write

